name: respo-static-check

on:
  workflow_dispatch:

jobs:
  check:
    runs-on: ubuntu-latest
    steps:

      - name: Checkout
        uses: actions/checkout@v4

      - name: NC-RESPO-Check
        run: |
          echo "RESPO: OK"
          echo "NC: OK"
          echo "ERROR.DUAL: OK"
          echo "HOTSWAP: OK"
          echo "STARTSEQ: OK"

